<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Адаптация сотрудников{% endblock %}</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            font-family: Arial, sans-serif;
            background: linear-gradient(135deg, #667eea, #764ba2);
            min-height: 100vh;
            padding: 20px;
        }
        .container { max-width: 1000px; margin: 0 auto; }
        h1 { text-align: center; color: white; margin-bottom: 20px; }
        .card {
            background: white;
            border-radius: 20px;
            padding: 25px;
            margin-bottom: 20px;
        }
        .btn {
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 10px;
            cursor: pointer;
            text-decoration: none;
            display: inline-block;
        }
        .header-btns {
            text-align: right;
            margin-bottom: 20px;
        }
        .logout-btn {
            background: rgba(255,255,255,0.2);
            color: white;
            padding: 8px 16px;
            border-radius: 10px;
            text-decoration: none;
        }
        .user-name {
            color: white;
            margin-right: 15px;
        }
    </style>
    {% block extra_css %}{% endblock %}
</head>
<body>
    <div class="container">
        <div class="header-btns">
            {% if user.is_authenticated %}
                <span class="user-name">👤 {{ user.username }}</span>
                <a href="{% url 'profile' %}" class="logout-btn">Профиль</a>
                <a href="{% url 'courses' %}" class="logout-btn">Курсы</a>
                <a href="{% url 'admin:index' %}" class="logout-btn">Админка</a>
                <a href="{% url 'logout' %}" class="logout-btn">Выйти</a>
            {% else %}
                <a href="{% url 'login' %}" class="logout-btn">Войти</a>
                <a href="{% url 'register' %}" class="logout-btn">Регистрация</a>
            {% endif %}
        </div>
        <h1>Система адаптации сотрудников</h1>
        
        {% if messages %}
            {% for message in messages %}
                <div class="card" style="background: #e8f5e9; text-align: center;">{{ message }}</div>
            {% endfor %}
        {% endif %}
        
        {% block content %}{% endblock %}
    </div>
</body>
</html>